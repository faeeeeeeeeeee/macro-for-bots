services:
  - type: web
    name: arras-ws-relay
    runtime: node
    plan: free
    rootDir: ws-relay
    buildCommand: npm install
    startCommand: node relay.js
    envVars:
      - key: PORT
        value: 10000
      - key: MAX_CONNECTIONS
        value: 20
